<%= form_with(model: restaurent, local: true) do |form| %>
  <% if restaurent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurent.errors.count, "error") %> prohibited this restaurent from being saved:</h2>

      <ul>
        <% restaurent.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name,class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :details %>
    <%= form.text_area :details,class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location,class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
